<aside
  id="aside-info"
  class="h-fit w-full space-y-4 rounded-xl bg-gradient-primary p-4 md:w-[18rem]"
>
  <!-- Project status -->
  <section>
    <h3 class="text-lg font-semibold tracking-wide uppercase">Estado</h3>
    <div class="flex items-center gap-2">
      <div id="modal-status-indicator" class="h-4 w-4 rounded-full"></div>
      <p id="modal-status" class="text-sm font-medium text-secondary capitalize">
        <!-- Status will be injected here -->
      </p>
    </div>
  </section>

  <!-- Client -->
  <section>
    <h3 class="text-lg font-semibold tracking-wide uppercase">Cliente</h3>
    <p id="modal-customer" class="text-sm font-medium text-secondary">
      <!-- Customer will be injected here -->
    </p>
  </section>

  <!-- Duration (if available) -->
  <section id="modal-duration-container" class="hidden">
    <h3 class="text-lg font-semibold tracking-wide uppercase">Duración</h3>
    <p id="modal-duration" class="text-sm font-medium text-secondary">
      <!-- Duration will be injected here -->
    </p>
  </section>

  <!-- Technologies -->
  <!-- links -->
</aside>
