---
import type { IconName } from 'consts/icons';

interface Props {
  name: IconName;
  class?: string;
  size?: 'sm' | 'md' | 'lg' | 'xl';
  [key: string]: any;
}

const { name, class: className = '', size = 'md', ...props } = Astro.props;

const sizeClasses = {
  sm: 'w-4 h-4',
  md: 'w-6 h-6',
  lg: 'w-8 h-8',
  xl: 'w-12 h-12',
};

// Importar una sola vez y crear mapa para acceso rápido
const { icons } = await import('consts/icons.ts');
const IconMap = new Map(Object.entries(icons));
const IconComponent = IconMap.get(name);
---

{
  IconComponent && (
    <IconComponent class:list={[size ? sizeClasses[size] : '', className]} {...props} />
  )
}
