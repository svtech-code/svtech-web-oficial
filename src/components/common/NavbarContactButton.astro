<a
  href="#contact"
  aria-label="ir a la sección de contacto"
  class="relative flex w-32 items-center justify-center overflow-hidden rounded-lg p-2 text-lg text-gradient-secondary duration-500 hover:scale-105 hover:shadow-sm hover:shadow-tertiary focus:ring-2 focus:ring-primary focus:ring-offset-2 focus:outline-none"
>
  Contacto
</a>

<style>
  @reference "../../styles/global.css";

  a {
    @apply bg-gradient-to-r from-primary via-[#00D0FF] to-primary;
    background-size: 200% 100%;
    animation: shimmer 3s linear infinite;
  }

  a::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    @apply bg-gradient-to-r from-transparent via-primary/40 to-transparent;
    transform: rotate(30deg);
    animation: shine 3s linear infinite;
  }
</style>
