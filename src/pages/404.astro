---
import ServiceLayout from 'layouts/ServiceLayout.astro';
---

<ServiceLayout title=" 404 - Página no encontrada - Sv Tech">
  <section class="flex h-screen w-full flex-col items-center justify-center px-4">
    <header class="flex flex-col items-center justify-center gap-2 py-8 text-center">
      <p
        id="attempted-url"
        class="py-8 text-center text-4xl text-tertiary/80"
        aria-label="URL solicitada"
        aria-live="polite"
      >
      </p>

      <h1 class="text-8xl font-bold text-primary">404</h1>
      <h2 class="text-6xl font-semibold text-primary/80">Página no encontrada</h2>
    </header>

    <main class="flex flex-col items-center justify-center">
      <p class="mb-8 max-w-xl text-center text-xl font-medium text-secondary">
        Lo sentimos, el sitio al cual intenta acceder no existe, por favor, intenta más tarde o
        visita un sitio válido.
      </p>

      <nav>
        <a
          href="/"
          class="item-center flex h-10 w-30 justify-center rounded-lg border border-primary bg-primary p-2 text-sm font-medium text-gradient-secondary duration-300 hover:scale-105 sm:h-12 sm:w-52 sm:text-lg"
        >
          Ir al inicio
        </a>
      </nav>
    </main>
  </section>
</ServiceLayout>

<script>
  // Captura y visualización de la URL solicitada
  document.addEventListener('astro:page-load', (): void => {
    const urlElement: HTMLElement | null = document.getElementById('attempted-url');

    if (urlElement) urlElement.textContent = `Intentaste acceder a: ${window.location.pathname}`;
  });
</script>
